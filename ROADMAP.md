# 의류 자동 분류기 로드맵

## 목표

사용자가 의류를 한 번 등록하면, 이후 카메라에 들어온 동일 또는 유사 의류를 찾아
`색이 진한 외출복`, `색이 연한 외출복`, `내복` 중 등록된 분류로 안내한다.
Raspberry Pi 5에서 카메라 입력부터 분류 결과 표시까지 독립적으로 동작하는 것이
첫 번째 출시 목표(MVP)다.

## 현재 상태

- Raspberry Pi용 정지 이미지·영상 캡처 유틸리티가 구현되어 있다.
- 임베딩 모델, 의류 등록 흐름, 저장소, 유사도 검색, 사용자 화면은 아직 구현 전이다.
- 데이터베이스와 검색 엔진은 미선정 상태다.

## 단계별 계획

### 0. 요구사항 확정

결정할 항목:

- 의류 한 벌의 등록 사진 수와 촬영 조건(배경, 거리, 조명)
- 한 번에 카메라에 들어오는 의류 수 및 인식 실패 시 동작
- 결과를 표시하거나 전달할 방식(터미널, 화면, LED, 모터 제어 등)
- 목표 정확도와 허용 응답 시간

완료 기준: 위 항목을 `README.md` 또는 별도 요구사항 문서에 기록하고 MVP 범위를 확정한다.

### 1. 개발·실행 환경 정비

- Raspberry Pi 5에서 Picamera2와 프로젝트 가상환경이 함께 동작하는지 확인한다.
- Python, PyTorch/Torchvision, OpenCV의 Pi 호환 버전을 고정한다.
- `pytest`, 포매터, 린터를 추가하고 로컬과 Pi에서 실행 가능한 기본 검증 명령을 만든다.
- 실제 카메라 없이도 테스트할 수 있도록 샘플 이미지를 준비한다.

완료 기준: Pi에서 사진 한 장을 캡처하고, 개발 PC에서는 샘플 이미지 기반 테스트가 통과한다.

### 2. 이미지 전처리 및 임베딩 추출

- MobileNet V3-Small을 우선 후보로 선정하고, 분류 헤드 이전의 특징 벡터를 추출한다.
- RGB 입력, 리사이즈, 정규화 등 모델별 전처리를 하나의 모듈로 캡슐화한다.
- L2 정규화된 `float32` 임베딩과 모델·전처리 버전을 반환한다.
- 배경·조명·회전 변화가 있는 의류 샘플로 유사도 분포를 측정한다.

완료 기준: 동일 의류의 임베딩 유사도가 다른 의류보다 일관되게 높고, 추론 시간이 목표 범위 안에 든다.

### 3. 등록 데이터와 저장소 설계

- 의류 ID, 사용자 선택 분류, 등록 일시, 원본 이미지 경로, 임베딩, 모델 버전을 저장한다.
- 초기 MVP는 SQLite + NumPy 배열 직렬화로 시작해 설치·백업 부담을 낮춘다.
- 등록, 조회, 수정, 삭제 API를 만든다.
- 모델 또는 전처리 버전이 바뀌었을 때 재임베딩할 수 있게 설계한다.

완료 기준: 의류를 등록한 뒤 프로그램을 재시작해도 메타데이터와 임베딩을 정확히 조회한다.

### 4. 유사도 검색 및 분류 정책

- 코사인 유사도로 상위 후보를 검색한다.
- 최고 유사도와 1·2위 차이를 함께 사용해 신뢰도를 판단한다.
- 임계값 미만이면 기존 의류로 단정하지 않고 `미등록/재확인 필요`로 반환한다.
- 등록된 분류는 가장 유사한 의류의 분류를 따른다.

완료 기준: 소규모 검증 세트에서 정확도, 오분류율, 미등록 의류 오인식률을 측정하고 임계값을 문서화한다.

### 5. 카메라 통합과 사용자 흐름

- 등록 모드: 촬영 → 미리보기/확인 → 분류 선택 → 저장.
- 분류 모드: 촬영 → 전처리/검색 → 결과와 신뢰도 표시.
- 카메라 초기화 실패, 빈 프레임, 흔들린 사진, DB 오류를 사용자에게 이해 가능한 메시지로 처리한다.
- 반복 촬영 시 카메라와 메모리를 안정적으로 해제한다.

완료 기준: Pi에서 등록부터 재인식까지의 전체 흐름을 수동 개입 없이 연속 실행한다.

### 6. 현장 검증 및 성능 개선

- 실제 설치 위치에서 조명, 그림자, 옷의 접힘, 계절별 소재를 포함해 데이터를 수집한다.
- 오인식 사례를 유형별로 기록하고, 전처리·임계값·등록 사진 수를 조정한다.
- 필요할 때만 더 가벼운 모델, ONNX/TFLite 변환 또는 제한적인 파인튜닝을 검토한다.
- 데이터 백업·복원 및 개인정보 처리 기준을 정한다.

완료 기준: 정한 운영 조건에서 목표 정확도와 응답 시간을 만족하고, 실패 상황도 안전하게 처리한다.

## MVP 우선순위

1. 단일 의류·단일 사용자·세 가지 수동 분류만 지원한다.
2. MobileNet V3-Small + 코사인 유사도 + SQLite를 기본 조합으로 구현한다.
3. 결과 표시는 우선 터미널 또는 간단한 화면으로 제한한다.
4. 다중 의류 검출, 자동 분류 라벨 생성, 클라우드 동기화, 파인튜닝은 MVP 이후로 미룬다.

## 핵심 지표

| 지표 | 초기 목표 | 측정 방법 |
| --- | --- | --- |
| 등록 의류 재인식률 | 90% 이상 | 조건 변화가 있는 재촬영 이미지로 평가 |
| 미등록 의류 오인식률 | 5% 이하 | 등록되지 않은 의류를 별도 평가 |
| 분류 응답 시간 | 2초 이하 | 촬영 완료부터 결과 표시까지 측정 |
| 등록 데이터 보존 | 재시작 후 100% | 저장·종료·재실행 후 조회 테스트 |

## 다음 작업

1. 단계 0의 촬영 조건과 MVP 입출력을 확정한다.
2. Pi 5에서 지원되는 PyTorch/Torchvision 조합을 검증한다.
3. 임베딩 추출 모듈과 샘플 이미지 기반 단위 테스트를 구현한다.
